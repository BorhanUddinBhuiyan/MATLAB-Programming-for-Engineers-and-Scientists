%Random Squence Initialization:
%{
> When we start MATLAB and call 'rand', it always returns the same exact
number: 0.8147 
> "Pseudo" random number generator: initialized at startupp and it
generates the exact same sequence of nmbers evey time
> Repeatability for testing the program the exact same sequence of numbers everytim 

%}